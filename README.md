###Mini Laravel Blog

####Installation:

PHP dependencies

``composer install``;

Migrate the DB

```php artisan migrate```

Build the assets

```npm i && npm run prod```

Serve

```php artisan serve```